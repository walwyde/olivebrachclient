import React from 'react'

const NotFound = () => {
  return (
    <div>NotFound 
      <button onClick={() => history.back()} className="btn">Go Back</button>
    </div>
  )
}

export default NotFound